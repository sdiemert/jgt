language: java

install:
    - scripts/install-dependencies.sh
    - mvn install -DskipTests=true -Dmaven.javadoc.skip=true -B -V

script:
  - sudo rm /etc/mavenrc
  - export MAVEN_OPTS="-Djava.library.path=./lib"
  - mvn test -B
